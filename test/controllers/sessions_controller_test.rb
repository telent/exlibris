require "minitest_helper"

class SessionsControllerTest < MiniTest::Rails::Controller
  before do
    @controller = SessionsController.new
  end

  it "must be a real test" do
    flunk "Need real tests"
  end
end
