<h1>Listing shelves</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Owner</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @shelves.each do |shelf| %>
  <tr>
    <td><%= shelf.name %></td>
    <td><%= shelf.owner %></td>
    <td><%= link_to 'Show', shelf %></td>
    <td><%= link_to 'Edit', edit_shelf_path(shelf) %></td>
    <td><%= link_to 'Destroy', shelf, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Shelf', new_shelf_path %>
